[metadata]
license_files = LICENSE

[tool:pytest]
python_files = *test*.py
testpaths = tests
norecursedirs=schwab build dist docs htmlcov

[coverage:run]
branch = True

[coverage:report]
# Keep this list to lines that actually occur. `except ImportError`,
# `def __repr__`, `raise DeprecationWarning` and `deprecate` were all here and
# matched nothing in schwab/ -- the first of those is the one that matters,
# since it would silently drop any future ImportError handler out of coverage.
exclude_lines =
    pragma: no cover
    raise NotImplementedError
